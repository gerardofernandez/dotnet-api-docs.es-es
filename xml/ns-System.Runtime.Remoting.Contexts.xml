<Namespace Name="System.Runtime.Remoting.Contexts">
  <Docs>
    <summary><span data-ttu-id="a86d9-101">El espacio de nombres <see cref="N:System.Runtime.Remoting.Contexts" /> contiene objetos que definen los contextos en los que residen los objetos.</span><span class="sxs-lookup"><span data-stu-id="a86d9-101">The <see cref="N:System.Runtime.Remoting.Contexts" /> namespace contains objects that define the contexts all objects reside within.</span></span> <span data-ttu-id="a86d9-102">Un contexto es una serie ordenada de propiedades que define un entorno para los objetos que contiene.</span><span class="sxs-lookup"><span data-stu-id="a86d9-102">A context is an ordered sequence of properties that defines an environment for the objects within it.</span></span> <span data-ttu-id="a86d9-103">Los contextos se crean durante el proceso de activación de los objetos configurados para requerir determinados servicios automáticos, como la sincronización, las transacciones, la activación just-in-time (JIT), la seguridad, etc.</span><span class="sxs-lookup"><span data-stu-id="a86d9-103">Contexts are created during the activation process for objects that are configured to require certain automatic services such synchronization, transactions, just-in-time (JIT) activation, security, and so on.</span></span> <span data-ttu-id="a86d9-104">En un contexto pueden residir varios objetos.</span><span class="sxs-lookup"><span data-stu-id="a86d9-104">Multiple objects can live inside a context.</span></span></summary>
    <remarks>
      <format type="text/markdown"><![CDATA[  
  
## Remarks  
 <span data-ttu-id="a86d9-105">Cuando se crea un nuevo objeto,.NET Framework busca un contexto compatible o crea un nuevo contexto para el objeto.</span><span class="sxs-lookup"><span data-stu-id="a86d9-105">Whenever a new object is created, the.NET Framework finds a compatible context or creates a new context for the object.</span></span> <span data-ttu-id="a86d9-106">Después de que un objeto se coloca en un contexto, permanece en él para siempre.</span><span class="sxs-lookup"><span data-stu-id="a86d9-106">After an object is placed in a context, it stays in it for life.</span></span> <span data-ttu-id="a86d9-107">Las clases que pueden enlazarse a un contexto se denominan clases enlazadas a un contexto.</span><span class="sxs-lookup"><span data-stu-id="a86d9-107">Classes that can be bound to a context are called context-bound classes.</span></span> <span data-ttu-id="a86d9-108">Cuando se tiene acceso desde otro contexto, se hace referencia a estas clases enlazadas a un contexto directamente mediante un proxy.</span><span class="sxs-lookup"><span data-stu-id="a86d9-108">When accessed from another context, these context-bound classes are referenced directly using a proxy.</span></span> <span data-ttu-id="a86d9-109">Una llamada de un objeto en un contexto a un objeto en otro contexto pasará a través de un proxy de contexto y verse afectada por la directiva implementada por las propiedades de contexto combinado.</span><span class="sxs-lookup"><span data-stu-id="a86d9-109">A call from an object in one context to an object in another context will go through a context proxy and be affected by the policy implemented by the combined context properties.</span></span>  
  
 ]]></format>
    </remarks>
  </Docs>
</Namespace>